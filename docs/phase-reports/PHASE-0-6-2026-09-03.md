# PromptForge — Phasenbericht Phase 0–6 (v0.1 Bootstrap)

Datum: 2026-09-03 · Repo: `~/Projects/priv/forge/PromptForge` (uncommitted)

## Phase 0 — Repository Inspection
- `~/.prompt-forge` existierte nicht (Auftragskopf-Prämisse falsch); CWD war ein
  leeres, heute angelegtes Verzeichnis → Projekt-Root = CWD; `~/.prompt-forge`
  bleibt Runtime-User-Home (Spec §14). Toolchains: rustc/cargo 1.98, uv 0.12.7,
  Python 3.12.14/3.14.7 (Homebrew).

## Phase 1 — Architektur
- `docs/architecture.md`: Entscheidungen D-01…D-12 (Monorepo, Rust-Core /
  Python-AI-Schicht, transaktionale PyO3-Boundary, Embed-Mode statt cdylib —
  `auto-initialize` vs. `extension-module` schließen sich im selben
  Feature-Unifikationsraum aus, siehe Doku, LLM_ENDPOINT-Abstraktion,
  Prompt-IR, Optimizer-Pass-Pipeline, Verify-Retry, Redaction).
- any-llm-Recherche (GitHub-README/pyproject): PyPI-Paket heißt
  **`any-llm-sdk`**, Modul `any_llm`, `AnyLLM.create_openai_compatible(...)`
  für beliebige OpenAI-kompatible Endpunkte — in `python/.../provider_anyllm.py`
  gekapselt (Import lazy).

## Phase 2 — Bootstrap
- Cargo-Workspace (6 Crates: pf-core/-engine/-bridge/-cli/-tui/-service),
  `.cargo/config.toml`-Hinweis + `scripts/env.sh` (PYO3_PYTHON), uv-Projekt
  `python/` (pyproject, hatchling), rustfmt/Editorconfig/Gitignore, Logging-
  Grundgerüst, PyO3 gegen Homebrew-Python 3.14 gebaut.

## Phase 3 — Vertical Slice (ohne Cloud-API)
- CLI → Engine → IR → Expansion → Optimize (Rust-Passes + Python/Mock-LLM) →
  Verify → CLI-Output. Deterministischer Modus ohne LLM; E2E über
  eingebettetes Python + Python-MockLLM grün (`tests/e2e_python_bridge.rs`).

## Phase 4 — any-llm (Code vollständig, Live-Test offen)
- `provider_anyllm.py` implementiert; `uv`-Installation war durch das
  Approval-Gate blockiert → echte LLM-Calls nach Freigabe `uv sync` prüfen.

## Phase 5 — TUI
- `pf-tui` (ratatui): Input, Pipeline-Status, Token/Verifikation, Preview,
  Copy/Save/Quit; kompiliert, Unit-Tests grün (interaktiver Test offen).

## Phase 6 — Service
- `pf-service` (axum): `/v1/compile|optimize|verify|execute`, `/v1/health`,
  Fehler-Envelope; Router-Tests grün, `prompt-forge serve` startet.

## Tests/Gates (echte Läufe)
- `cargo test --workspace`: pf-core 37, pf-engine 21, pf-service 3, pf-tui 2,
  pf-bridge 1, e2e_python_bridge 1 — alle ok.
- `cargo clippy --workspace --all-targets`: keine Errors/Warnings.
- `cargo fmt --check`: sauber.
- Python: 19 Tests ok (System-Python 3.14, ohne any-llm/loguru — lazy Imports).
- CLI-Smoke: init, stdin, `--json` (Schema ok, verdict pass), Datei+`-o`,
  Python-Mock-LLM über PyO3 im Binary — exit 0.

## Einschränkungen
- `uv venv`/`uv add` blockiert (Approval-Gate, User abwesend) → any-llm/loguru
  nicht installiert; Live-LLM-Call nicht ausgeführt. Kein Commit/Push
  (Konvention: Freigabe erforderlich).
